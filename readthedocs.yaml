version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: source/conf.py  # Path to your Sphinx config

python:
  install:
    - requirements: requirements.txt  # Install dependencies
